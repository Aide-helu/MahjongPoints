# 学习文档

## 维护规则

- 只有明确要求写入时，才更新本文档。
- 每次记录包含提问和整理后的知识点。
- 未明确要求写入时，不修改本文档。

## 记录

### 2026-06-07：DockPanel 是什么，WPF 里面有吗，具体是什么效果？

**问题**

DockPanel 是什么，WPF 里面有吗，具体是什么效果？

**知识点**

DockPanel 是一种布局容器，用来把子控件停靠到父容器的上、下、左、右四个边缘。它适合做工具栏、状态栏、侧边栏、顶部导航等典型布局。

WPF 里有 DockPanel。Avalonia 里也有 DockPanel，概念和用法基本一致。

DockPanel 的核心属性是 `DockPanel.Dock`，写在子控件上：

```xml
<DockPanel>
    <Button DockPanel.Dock="Top" Content="顶部工具栏"/>
    <Button DockPanel.Dock="Left" Content="左侧菜单"/>
    <TextBlock Text="剩余内容区域"/>
</DockPanel>
```

效果是：

- `DockPanel.Dock="Top"` 的控件贴在顶部。
- `DockPanel.Dock="Bottom"` 的控件贴在底部。
- `DockPanel.Dock="Left"` 的控件贴在左边。
- `DockPanel.Dock="Right"` 的控件贴在右边。
- 默认最后一个子控件会填满剩余空间。

DockPanel 有一个重要属性：`LastChildFill`。默认通常为 `True`，表示最后一个子控件不再按 Dock 方向摆放，而是自动占满剩余区域。如果设置为 `False`，最后一个子控件也会按自己的 `DockPanel.Dock` 正常停靠。

在当前项目里有类似写法：

```xml
<DockPanel LastChildFill="True">
    <StackPanel DockPanel.Dock="Left" Orientation="Horizontal"/>
    <TextBlock TextAlignment="Right"/>
</DockPanel>
```

这里的效果是：左侧 `StackPanel` 停靠到左边，后面的 `TextBlock` 作为最后一个子控件，占据剩余空间，所以它可以在右侧区域显示状态文字。

### 2026-06-07：Grid 里面的 ColumnDefinitions 和 ColumnSpacing 是什么意思，WPF 里面好像不是这样写的？

**问题**

Grid 里面的 `ColumnDefinitions` 和 `ColumnSpacing` 是什么意思，WPF 里面好像不是这样写的。

**知识点**

`Grid` 是表格型布局容器，可以把界面切成多行、多列。`ColumnDefinitions` 用来定义列，`RowDefinitions` 用来定义行。

在 Avalonia 里，可以用简写：

```xml
<Grid ColumnDefinitions="3*,2*" ColumnSpacing="16">
    <Border Grid.Column="0"/>
    <ScrollViewer Grid.Column="1"/>
</Grid>
```

这里的意思是：

- `ColumnDefinitions="3*,2*"`：定义两列，第一列占剩余宽度的 3 份，第二列占剩余宽度的 2 份。
- `ColumnSpacing="16"`：两列之间留 16 像素间距。
- `Grid.Column="0"`：控件放到第 0 列，也就是第一列。
- `Grid.Column="1"`：控件放到第 1 列，也就是第二列。

`*` 是比例单位，不是固定像素。比如 `3*,2*` 表示总共 5 份，第一列占 3 份，第二列占 2 份。

WPF 里也有 `Grid`、`ColumnDefinitions`、`RowDefinitions`，但常见写法是属性元素语法：

```xml
<Grid>
    <Grid.ColumnDefinitions>
        <ColumnDefinition Width="3*"/>
        <ColumnDefinition Width="2*"/>
    </Grid.ColumnDefinitions>

    <Border Grid.Column="0"/>
    <ScrollViewer Grid.Column="1"/>
</Grid>
```

Avalonia 的 `ColumnDefinitions="3*,2*"` 是更短的写法，效果相当于 WPF 里写两个 `ColumnDefinition`。

另一个区别是：Avalonia 的 `Grid` 有 `ColumnSpacing` 和 `RowSpacing`，可以直接设置列间距、行间距。WPF 的原生 `Grid` 通常没有这个属性，如果要做列间距，常见做法是给子控件设置 `Margin`，或者额外插入一列作为间隔。

例如 WPF 里常见的间距写法：

```xml
<Grid>
    <Grid.ColumnDefinitions>
        <ColumnDefinition Width="3*"/>
        <ColumnDefinition Width="16"/>
        <ColumnDefinition Width="2*"/>
    </Grid.ColumnDefinitions>

    <Border Grid.Column="0"/>
    <ScrollViewer Grid.Column="2"/>
</Grid>
```

所以可以这样记：

`ColumnDefinitions` 负责“分几列、每列多宽”，`ColumnSpacing` 负责“列和列之间隔多远”。

### 2026-06-07：DataTemplate 是干什么的，为什么最后有很多一块一块的数据？

**问题**

下面这段 AXAML 里，`DataTemplate` 是干什么的？为什么最后界面上有很多一块一块的数据？这些块是怎么添加的？每一块的数据是怎么来的？

```xml
<ItemsControl ItemsSource="{Binding RecognizedTiles}">
    <ItemsControl.ItemsPanel>
        <ItemsPanelTemplate>
            <WrapPanel/>
        </ItemsPanelTemplate>
    </ItemsControl.ItemsPanel>
    <ItemsControl.ItemTemplate>
        <DataTemplate>
            <Border Width="86">
                <StackPanel>
                    <TextBlock Text="{Binding DisplayName}"/>
                    <TextBlock Text="{Binding Code}"/>
                    <TextBlock Text="{Binding ConfidenceText}"/>
                </StackPanel>
            </Border>
        </DataTemplate>
    </ItemsControl.ItemTemplate>
</ItemsControl>
```

**知识点**

`DataTemplate` 是“数据模板”，用来告诉 `ItemsControl`：集合里的每一条数据应该长成什么样。

`ItemsControl` 本身只知道要显示一组数据，不知道每条数据应该用按钮、文字、边框还是图片来显示。`DataTemplate` 就是给它的“单项显示模板”。

在这段代码里：

```xml
<ItemsControl ItemsSource="{Binding RecognizedTiles}">
```

`ItemsSource` 绑定到 ViewModel 里的 `RecognizedTiles` 集合。这个集合里每一个元素都是一张识别出的麻将牌，例如：

```csharp
new RecognizedMahjongTile("2m", "二万", 0.99)
new RecognizedMahjongTile("3m", "三万", 0.98)
new RecognizedMahjongTile("4m", "四万", 0.98)
```

`ItemsControl` 会遍历 `RecognizedTiles`。集合里有 13 个元素，就生成 13 个界面块。

每个界面块都使用同一个 `DataTemplate`：

```xml
<DataTemplate>
    <Border Width="86"
            Margin="0,0,8,8"
            Padding="8"
            BorderBrush="#D0D5DD"
            BorderThickness="1"
            Background="White"
            CornerRadius="4">
        <StackPanel Spacing="2">
            <TextBlock Text="{Binding DisplayName}"/>
            <TextBlock Text="{Binding Code}"/>
            <TextBlock Text="{Binding ConfidenceText}"/>
        </StackPanel>
    </Border>
</DataTemplate>
```

所以看到的“一块一块的数据”，其实就是这个 `Border` 被重复生成了很多次。

每一块的数据来自当前集合项。也就是说，在 `DataTemplate` 内部，`DataContext` 不再是整个 `MainWindowViewModel`，而是当前那一张牌 `RecognizedMahjongTile`。

因此：

- `{Binding DisplayName}` 绑定当前牌的显示名，例如“二万”。
- `{Binding Code}` 绑定当前牌的编码，例如 `2m`。
- `{Binding ConfidenceText}` 绑定当前牌的识别置信度文本，例如 `99%`。

这些数据是在 ViewModel 里添加进去的：

```csharp
RecognizedTiles.Clear();
foreach (var tile in recognitionResult.Tiles)
{
    RecognizedTiles.Add(tile);
}
```

`RecognizedTiles` 是 `ObservableCollection<RecognizedMahjongTile>`。它的特点是：添加、删除、清空元素时，会通知界面自动刷新。因此代码里 `RecognizedTiles.Add(tile)` 执行一次，界面上就会多生成一个对应的模板块。

`WrapPanel` 决定这些块怎么排列：

```xml
<ItemsPanelTemplate>
    <WrapPanel/>
</ItemsPanelTemplate>
```

它的效果是从左到右排列，宽度不够时自动换行。真正决定“每一块长什么样”的是 `DataTemplate`，决定“这些块怎么排”的是 `ItemsPanelTemplate` 里的 `WrapPanel`。

可以这样记：

`ItemsSource` 负责提供数据集合，`DataTemplate` 负责定义每条数据的界面样子，`ItemsPanelTemplate` 负责定义这些条目怎么排列。

### 2026-06-07：为什么 Rider 建议 `using var imageStream = File.OpenRead(imagePath);` 使用 `await using`？

**问题**

`using var imageStream = File.OpenRead(imagePath);` 为什么 Rider 建议使用 `await using`？两者有什么区别？前者是被废弃了吗？

**知识点**

`using var` 没有被废弃。它仍然是 C# 中正常、推荐的资源释放写法之一。

这行代码：

```csharp
using var imageStream = File.OpenRead(imagePath);
```

表示：创建一个文件流 `imageStream`，当前作用域结束时自动调用它的同步释放方法 `Dispose()`。

大概等价于：

```csharp
FileStream imageStream = File.OpenRead(imagePath);
try
{
    // 使用 imageStream
}
finally
{
    imageStream.Dispose();
}
```

`await using` 是另一种释放方式：

```csharp
await using var imageStream = File.OpenRead(imagePath);
```

它表示：当前作用域结束时调用异步释放方法 `DisposeAsync()`，并等待释放完成。

大概等价于：

```csharp
FileStream imageStream = File.OpenRead(imagePath);
try
{
    // 使用 imageStream
}
finally
{
    await imageStream.DisposeAsync();
}
```

区别在于：

- `using var` 调用 `Dispose()`，同步释放资源。
- `await using` 调用 `DisposeAsync()`，异步释放资源。
- `using var` 适合普通资源或释放成本很低的资源。
- `await using` 适合支持异步释放的资源，尤其是释放时可能涉及异步 I/O 的资源。

`FileStream` 在现代 .NET 中实现了 `IAsyncDisposable`，所以 Rider 看到它支持 `DisposeAsync()`，并且当前代码在 `async` 方法里，就可能建议改成 `await using`。

但这不是因为 `using var` 过时，而是 Rider 认为：既然当前方法已经是异步方法，并且对象支持异步释放，使用 `await using` 可以避免在释放阶段执行同步 I/O。

在当前项目这段代码里：

```csharp
using var imageStream = File.OpenRead(imagePath);
ReplacePreviewImage(new Bitmap(imageStream));
```

使用 `using var` 是可以的，因为这里马上把文件流交给 `Bitmap` 构造函数读取，文件流的生命周期很短。即使改成 `await using`，实际收益也不一定明显。

如果要按 Rider 建议写，可以改成：

```csharp
await using var imageStream = File.OpenRead(imagePath);
ReplacePreviewImage(new Bitmap(imageStream));
```

前提是当前方法本身是 `async` 方法，例如：

```csharp
public async Task LoadAndRecognizeAsync(...)
```

可以这样记：

`using var` 是“同步释放”，`await using` 是“异步释放”。前者没有废弃，后者只是更适合支持异步释放并且释放过程可能有异步 I/O 的对象。

**补充：同步代码里 `using` 还有什么意义？**

同步代码本身当然是按顺序执行的，但 `using` 的意义不是“让代码同步执行”，而是“保证资源被释放”。

例如：

```csharp
using var imageStream = File.OpenRead(imagePath);
ReplacePreviewImage(new Bitmap(imageStream));
```

这里 `using var` 的核心作用是：当前作用域结束时，自动调用：

```csharp
imageStream.Dispose();
```

它等价于编译器帮我们写了一个 `try/finally`：

```csharp
FileStream imageStream = File.OpenRead(imagePath);
try
{
    ReplacePreviewImage(new Bitmap(imageStream));
}
finally
{
    imageStream.Dispose();
}
```

所以即使中间发生异常，`Dispose()` 也会执行。

如果不写 `using`：

```csharp
var imageStream = File.OpenRead(imagePath);
ReplacePreviewImage(new Bitmap(imageStream));
```

那么文件流什么时候释放就不明确了。理论上垃圾回收器以后可能会回收它，但时间不确定。在这段时间里可能出现：

- 文件句柄没有及时关闭。
- 图片文件被占用，不能删除或覆盖。
- 多次打开文件后资源占用变多。
- 出异常时更容易忘记手动释放。

所以 `using` 的价值是“确定性释放资源”，不是“控制代码是否同步执行”。

可以这样理解：

```text
同步代码：代码一行一行执行。
using var：同步地、确定地释放资源。
await using：异步地、确定地释放资源。
```

`using var` 中的“同步”指的是释放资源时调用同步的 `Dispose()`，这一步会在当前线程完成。`await using` 指的是释放资源时调用异步的 `DisposeAsync()`，这一步可以 `await`，更适合释放过程可能涉及异步 I/O 的对象。

**补充：手动释放是不是等价于 `using`？**

如果代码一定能正常执行到手动释放那一行，那么手动调用 `Dispose()` 的效果和 `using` 很接近。

例如：

```csharp
var imageStream = File.OpenRead(imagePath);
ReplacePreviewImage(new Bitmap(imageStream));
imageStream.Dispose();
```

如果中间没有异常，这确实会释放文件流。

但问题是：如果中间这行抛异常：

```csharp
ReplacePreviewImage(new Bitmap(imageStream));
```

那么后面的：

```csharp
imageStream.Dispose();
```

就不会执行，文件流可能没有及时释放。

所以更严谨的手动写法应该是：

```csharp
var imageStream = File.OpenRead(imagePath);
try
{
    ReplacePreviewImage(new Bitmap(imageStream));
}
finally
{
    imageStream.Dispose();
}
```

这才和 `using var` 基本等价。

也就是说：

```csharp
using var imageStream = File.OpenRead(imagePath);
ReplacePreviewImage(new Bitmap(imageStream));
```

本质上就是编译器帮我们生成了类似 `try/finally` 的代码。

可以这样记：

```text
手动 Dispose()：正常执行到那一行才会释放。
try/finally + Dispose()：不管中间是否异常，都会释放。
using var：编译器自动生成 try/finally + Dispose()。
```

所以手动释放不是不能用，但容易漏掉异常场景。大多数情况下，写 `using var` 更短、更安全，也更符合 C# 习惯。
